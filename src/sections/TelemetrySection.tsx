import { useApp } from '../AppContext';import { SignalTable, TelemetryPlot, TrackMap } from '../TelemetryVisuals';
export function TelemetrySection(){const{snapshot,history}=useApp();return <section id="telemetry" className="section telemetry-section"><div className="section-shell"><div className="section-heading"><div><span>02 / LIVE TELEMETRY</span><small>312 SYNCHRONIZED CHANNELS</small></div><h2>SEE THE LAP<br/><em>BEFORE IT ENDS.</em></h2><p>Continuous signal interpretation turns raw vehicle behavior into an immediately actionable engineering picture.</p></div><div className="telemetry-layout"><div className="telemetry-main"><div className="module-head"><span>LIVE SIGNAL COMPOSITE / 11.5 SECOND WINDOW</span><div><i/> STREAMING <b>160 MS</b></div></div><TelemetryPlot history={history}/><div className="telemetry-kpis"><article><span>LAT G</span><strong>{snapshot.lateralG.toFixed(1)}</strong><small>G</small></article><article><span>LONG G</span><strong>{snapshot.longitudinalG.toFixed(1)}</strong><small>G</small></article><article><span>ERS</span><strong>{snapshot.ers}</strong><small>%</small></article><article><span>FUEL</span><strong>{snapshot.fuel.toFixed(1)}</strong><small>KG</small></article></div></div><aside className="telemetry-side"><TrackMap sector={snapshot.sector} delta={snapshot.delta}/><SignalTable snapshot={snapshot}/></aside></div></div></section>}
