import { MonitorUp, RotateCcw } from 'lucide-react';import { useApp } from '../AppContext';
export function FinalSection(){const{reduced,toggleCommandMode}=useApp();return <section className="final-section"><div className="final-grid" aria-hidden="true"/><div className="section-shell final-section__content"><span>RACE INTELLIGENCE 3D / SYSTEM READY</span><h2>THE RACE IS WON<br/><em>BEFORE THE LIGHTS GO OUT.</em></h2><p>Performance becomes repeatable when every system, signal and decision is connected.</p><div><button className="primary-action" onClick={()=>document.getElementById('overview')?.scrollIntoView({behavior:reduced?'auto':'smooth'})}><RotateCcw size={16}/> RESTART EXPERIENCE</button><button className="text-action" onClick={toggleCommandMode}><MonitorUp size={15}/> ENTER COMMAND MODE</button></div></div><div className="start-lights" aria-hidden="true">{Array.from({length:5},(_,index)=><i key={index} style={{animationDelay:`${index*.14}s`}}/>)}</div></section>}
