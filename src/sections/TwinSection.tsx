import { ArrowRight, CheckCircle2 } from 'lucide-react';import { useApp } from '../AppContext';import { systems } from '../appData';import { RaceCarScene } from '../RaceCarScene';import type { ComponentId } from '../RaceCarScene';
export function TwinSection(){const{selected,setSelected,explode,snapshot,selectedSystem}=useApp();return <section id="twin" className="section twin-section"><div className="section-shell"><div className="section-heading"><div><span>03 / DIGITAL TWIN</span><small>COMPONENT-LEVEL INTELLIGENCE</small></div><h2>ONE CAR.<br/><em>SEVEN SYSTEMS.</em></h2><p>Select any system to bring its operational logic, live condition and engineering priorities into focus.</p></div><div className="twin-board"><div className="twin-board__visual"><RaceCarScene mode="engineering" view="top" selected={selected} explode={Math.max(.28,explode)} autoRotate={false} telemetry={snapshot} onSelect={setSelected}/><div className="twin-board__label"><span>DIGITAL TWIN / ACTIVE MESH</span><b>MODEL CONFIDENCE 98.7%</b></div></div><div className="system-selector">{(Object.keys(systems) as ComponentId[]).map(id=>{const item=systems[id];return <button key={id} className={selected===id?'is-active':''} onClick={()=>setSelected(id)}><span>{item.index}</span><div><strong>{item.title}</strong><small>{item.eyebrow}</small></div><ArrowRight size={14}/></button>})}</div><div className="twin-analysis"><span className="technical-label">{selectedSystem.eyebrow}</span><h3>{selectedSystem.title}</h3><p>{selectedSystem.description}</p><div className="analysis-number"><strong>{selectedSystem.primary}</strong><span>{selectedSystem.primaryLabel}</span></div><div className="analysis-bars"><article><span>MODEL CONFIDENCE</span><i><em style={{width:'98.7%'}}/></i><b>98.7%</b></article><article><span>OPERATING WINDOW</span><i><em style={{width:'84%'}}/></i><b>84%</b></article><article><span>PERFORMANCE HEADROOM</span><i><em style={{width:'62%'}}/></i><b>62%</b></article></div><div className="analysis-status"><CheckCircle2 size={16}/><div><span>ENGINEERING STATE</span><strong>{selectedSystem.status}</strong></div></div></div></div></div></section>}
